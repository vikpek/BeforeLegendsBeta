#pragma strict
/*
var baseStats : UnitData = UnitData();
//@HideInInspector
var gO : GameObject;

function Awake(){
	gO = this.transform.parent.gameObject;
	FillCarrier(gO);
}

function FillCarrier(carrier : GameObject) {
	var newEntry : MapObjectData = ScriptableObject.CreateInstance(MapObjectData) as MapObjectData;

	newEntry.battleStats.attack 		= baseStats.attack;		
	newEntry.battleStats.defense 		= baseStats.defense;		
	newEntry.battleStats.damage 		= baseStats.damage;		
	newEntry.battleStats.armor 			= baseStats.armor;		
	newEntry.battleStats.hitPoints 		= baseStats.hitPoints;		
	newEntry.battleStats.actionPoints 	= baseStats.actionPoints;		
	newEntry.battleStats.speed 			= baseStats.speed;		
	newEntry.battleStats.critStrike 	= baseStats.critStrike;		
	newEntry.battleStats.critBlock 		= baseStats.critBlock;

	carrier.GetComponent.<MapObjectCarrier>().data.Add(newEntry);
}*/